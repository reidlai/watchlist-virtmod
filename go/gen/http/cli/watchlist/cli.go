// Code generated by goa v3.23.4, DO NOT EDIT.
//
// watchlist HTTP client CLI support package
//
// Command:
// $ goa gen github.com/reidlai/ta-workspace/modules/watchlist/go/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	watchlistc "github.com/reidlai/ta-workspace/modules/watchlist/go/gen/http/watchlist/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"watchlist (list|add|remove)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "watchlist list --user-id \"Laudantium aut eum assumenda dolores tempore.\"" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		watchlistFlags = flag.NewFlagSet("watchlist", flag.ContinueOnError)

		watchlistListFlags      = flag.NewFlagSet("list", flag.ExitOnError)
		watchlistListUserIDFlag = watchlistListFlags.String("user-id", "REQUIRED", "")

		watchlistAddFlags      = flag.NewFlagSet("add", flag.ExitOnError)
		watchlistAddBodyFlag   = watchlistAddFlags.String("body", "REQUIRED", "")
		watchlistAddUserIDFlag = watchlistAddFlags.String("user-id", "REQUIRED", "")

		watchlistRemoveFlags      = flag.NewFlagSet("remove", flag.ExitOnError)
		watchlistRemoveSymbolFlag = watchlistRemoveFlags.String("symbol", "REQUIRED", "")
		watchlistRemoveUserIDFlag = watchlistRemoveFlags.String("user-id", "REQUIRED", "")
	)
	watchlistFlags.Usage = watchlistUsage
	watchlistListFlags.Usage = watchlistListUsage
	watchlistAddFlags.Usage = watchlistAddUsage
	watchlistRemoveFlags.Usage = watchlistRemoveUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "watchlist":
			svcf = watchlistFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "watchlist":
			switch epn {
			case "list":
				epf = watchlistListFlags

			case "add":
				epf = watchlistAddFlags

			case "remove":
				epf = watchlistRemoveFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "watchlist":
			c := watchlistc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = watchlistc.BuildListPayload(*watchlistListUserIDFlag)
			case "add":
				endpoint = c.Add()
				data, err = watchlistc.BuildAddPayload(*watchlistAddBodyFlag, *watchlistAddUserIDFlag)
			case "remove":
				endpoint = c.Remove()
				data, err = watchlistc.BuildRemovePayload(*watchlistRemoveSymbolFlag, *watchlistRemoveUserIDFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// watchlistUsage displays the usage of the watchlist command and its
// subcommands.
func watchlistUsage() {
	fmt.Fprintln(os.Stderr, `Manage user watchlist`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] watchlist COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: List implements list.`)
	fmt.Fprintln(os.Stderr, `    add: Add implements add.`)
	fmt.Fprintln(os.Stderr, `    remove: Remove implements remove.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s watchlist COMMAND --help\n", os.Args[0])
}
func watchlistListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] watchlist list", os.Args[0])
	fmt.Fprint(os.Stderr, " -user-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List implements list.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -user-id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "watchlist list --user-id \"Laudantium aut eum assumenda dolores tempore.\"")
}

func watchlistAddUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] watchlist add", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -user-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Add implements add.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -user-id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "watchlist add --body '{\n      \"on_hand\": false,\n      \"symbol\": \"Vel ea fugit et ab nobis.\"\n   }' --user-id \"Voluptatem sint quia soluta quo.\"")
}

func watchlistRemoveUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] watchlist remove", os.Args[0])
	fmt.Fprint(os.Stderr, " -symbol STRING")
	fmt.Fprint(os.Stderr, " -user-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Remove implements remove.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -symbol STRING: `)
	fmt.Fprintln(os.Stderr, `    -user-id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "watchlist remove --symbol \"Ex tempore occaecati.\" --user-id \"Recusandae quidem.\"")
}
