// Code generated by goa v3.23.4, DO NOT EDIT.
//
// watchlist HTTP client CLI support package
//
// Command:
// $ goa gen github.com/reidlai/ta-workspace/modules/watchlist/go/design
// --output goa_gen

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	watchlistc "github.com/reidlai/ta-workspace/modules/watchlist/go/goa_gen/gen/http/watchlist/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"watchlist (get-watchlist|add-watchlist-ticker|remove-watchlist-ticker)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "watchlist get-watchlist" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		watchlistFlags = flag.NewFlagSet("watchlist", flag.ContinueOnError)

		watchlistGetWatchlistFlags = flag.NewFlagSet("get-watchlist", flag.ExitOnError)

		watchlistAddWatchlistTickerFlags    = flag.NewFlagSet("add-watchlist-ticker", flag.ExitOnError)
		watchlistAddWatchlistTickerBodyFlag = watchlistAddWatchlistTickerFlags.String("body", "REQUIRED", "")

		watchlistRemoveWatchlistTickerFlags      = flag.NewFlagSet("remove-watchlist-ticker", flag.ExitOnError)
		watchlistRemoveWatchlistTickerSymbolFlag = watchlistRemoveWatchlistTickerFlags.String("symbol", "REQUIRED", "")
	)
	watchlistFlags.Usage = watchlistUsage
	watchlistGetWatchlistFlags.Usage = watchlistGetWatchlistUsage
	watchlistAddWatchlistTickerFlags.Usage = watchlistAddWatchlistTickerUsage
	watchlistRemoveWatchlistTickerFlags.Usage = watchlistRemoveWatchlistTickerUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "watchlist":
			svcf = watchlistFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "watchlist":
			switch epn {
			case "get-watchlist":
				epf = watchlistGetWatchlistFlags

			case "add-watchlist-ticker":
				epf = watchlistAddWatchlistTickerFlags

			case "remove-watchlist-ticker":
				epf = watchlistRemoveWatchlistTickerFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "watchlist":
			c := watchlistc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "get-watchlist":
				endpoint = c.GetWatchlist()
			case "add-watchlist-ticker":
				endpoint = c.AddWatchlistTicker()
				data, err = watchlistc.BuildAddWatchlistTickerPayload(*watchlistAddWatchlistTickerBodyFlag)
			case "remove-watchlist-ticker":
				endpoint = c.RemoveWatchlistTicker()
				data, err = watchlistc.BuildRemoveWatchlistTickerPayload(*watchlistRemoveWatchlistTickerSymbolFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// watchlistUsage displays the usage of the watchlist command and its
// subcommands.
func watchlistUsage() {
	fmt.Fprintln(os.Stderr, `Manage user watchlist`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] watchlist COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    get-watchlist: GetWatchlist implements getWatchlist.`)
	fmt.Fprintln(os.Stderr, `    add-watchlist-ticker: AddWatchlistTicker implements addWatchlistTicker.`)
	fmt.Fprintln(os.Stderr, `    remove-watchlist-ticker: RemoveWatchlistTicker implements removeWatchlistTicker.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s watchlist COMMAND --help\n", os.Args[0])
}
func watchlistGetWatchlistUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] watchlist get-watchlist", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `GetWatchlist implements getWatchlist.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "watchlist get-watchlist")
}

func watchlistAddWatchlistTickerUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] watchlist add-watchlist-ticker", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `AddWatchlistTicker implements addWatchlistTicker.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "watchlist add-watchlist-ticker --body '{\n      \"currency\": \"USD\",\n      \"exchange_mic\": \"XNAS\",\n      \"name\": \"Apple Inc\",\n      \"symbol\": \"AAPL\"\n   }'")
}

func watchlistRemoveWatchlistTickerUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] watchlist remove-watchlist-ticker", os.Args[0])
	fmt.Fprint(os.Stderr, " -symbol STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `RemoveWatchlistTicker implements removeWatchlistTicker.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -symbol STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "watchlist remove-watchlist-ticker --symbol \"Nobis quia consequatur totam et.\"")
}
