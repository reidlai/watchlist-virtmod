{
  "name": "@modules/watchlist-ts",
  "version": "0.0.1",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "lint": "echo 'Linting not configured yet'",
    "build": "echo 'Build not configured yet'",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}