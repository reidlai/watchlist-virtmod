$schema: 'https://moonrepo.dev/schemas/project.json'
id: 'watchlist-ts'

tasks:
  build:
    command: 'echo "No build script available"'
    outputs: []
    options:
      mergeOutputs: 'replace'
      mergeArgs: 'replace'
  
  lint:
    command: 'echo "No lint script available"'
    options:
      mergeArgs: 'replace'

  test:
    command: 'echo "No test script available"'
    options:
      mergeArgs: 'replace'
